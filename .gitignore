# Nuxt
.nuxt
.output
.data
dist

# Generated by the code-first GraphQL schema builder; the decorators are the
# source of truth.
# The GraphQL schema IS versioned: the front generates its types from this file
# instead of introspecting a live API, which is what lets introspection stay off
# in production and lets the front build without the back running.
# back/infrastructure/graphql/schema.gql

# Node
node_modules
*.log

# pnpm's package store when it cannot use the global one (containers)
.pnpm-store

# Env
.env

# Tests
test-results
playwright-report
coverage

# Python
__pycache__

# Local Netlify folder
.netlify

# Playwright
test-results
playwright-report
