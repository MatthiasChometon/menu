* text=auto eol=lf

# Container entrypoints: a CRLF shebang breaks the interpreter lookup
# (/bin/sh^M: bad interpreter).
*.sh text eol=lf

# Windows launchers, on the other hand, want CRLF.
*.bat text eol=crlf
*.ps1 text eol=crlf
